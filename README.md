# Docker compose for different application

# Docker compose up
docker-compose up

# Docker compose up in detached mode
docker-compose up -d

#Docker compose list launched container
docker-compose ps

# Docker compose down
docker-compose down
